apiVersion: 1

providers:
  - name: DOCSIS Monitor
    orgId: 1
    folder: DOCSIS
    folderUid: docsis
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
